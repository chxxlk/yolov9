# YOLOv9 baseline requirements for Python 3.12 / Google Colab
# Install with: python -m pip install -r requirements.txt
#
# Notes:
# - Colab usually ships with PyTorch preinstalled. If torch/torchvision are already present,
#   you can keep them, or reinstall matching versions explicitly if needed.
# - Optional integrations such as W&B, ClearML, Comet, ONNX, TensorRT, OpenVINO, TensorFlow,
#   and Triton are intentionally not required for core training/inference.

# Base ------------------------------------------------------------------------
gitpython>=3.1.43,<4.0
ipython>=8.18,<9.0
matplotlib>=3.8,<4.0
numpy>=1.26,<3.0
opencv-python>=4.10,<5.0
packaging>=24.1,<26.0
Pillow>=10.3,<12.0
psutil>=5.9,<8.0
PyYAML>=6.0.1,<7.0
requests>=2.31,<3.0
scipy>=1.11,<2.0
thop>=0.1.1.post2209072238
tqdm>=4.66,<5.0

# PyTorch ---------------------------------------------------------------------
# Keep these aligned if you install/upgrade manually on Colab.
torch>=2.4,<2.8
torchvision>=0.19,<0.23

# Logging ---------------------------------------------------------------------
tensorboard>=2.16,<3.0

# Plotting / analysis ---------------------------------------------------------
pandas>=2.1,<3.0
seaborn>=0.13,<1.0

# Data / augmentation ---------------------------------------------------------
albumentations>=1.4,<3.0
pycocotools>=2.0.8

# Optional integrations -------------------------------------------------------
# wandb>=0.17,<1.0
# clearml>=1.16,<2.0
# comet_ml>=3.45,<4.0

# Optional export / deploy ----------------------------------------------------
# onnx>=1.16
# onnxruntime>=1.18
# onnx-simplifier>=0.4.36
# openvino-dev>=2024.0
# tensorflow>=2.16
# tensorflowjs>=4.0
# paddlepaddle>=2.6
# tritonclient[all]>=2.50
